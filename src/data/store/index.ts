// this barrels all the proxy types we use in the store which act similarly to relational databases
// i.e. do not have nested objects but only ids/arrays thereof

export { StoreImage } from './StoreImage';
export { StoreSheet } from './StoreSheet';
export { StoreWork } from './StoreWork';
